import { Link } from 'react-router';
import axios from 'axios';
import React from 'react';

const Test = React.createClass({
  // handleClick() {
  //   this.props.handleAddToCart(this.props.product);
  // },

  render() {
    return (
      <section id="orders">
          <div id="orders-to-buy" className="four columns">


          </div>
      </section>
    )
  }
  });

export default Test;
